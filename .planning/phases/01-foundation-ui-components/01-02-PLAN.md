---
phase: 01-foundation-ui-components
plan: "02"
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/desktop/src/components/ui/settings-row.tsx
  - apps/desktop/src/components/ui/settings-section.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Settings row displays label on left, description below, and control on right in a single row"
    - "Settings section header visually groups related settings with title and optional description"
  artifacts:
    - path: "apps/desktop/src/components/ui/settings-row.tsx"
      provides: "Settings row with label + description + control layout"
      exports: ["SettingsRow"]
    - path: "apps/desktop/src/components/ui/settings-section.tsx"
      provides: "Settings section header for grouping"
      exports: ["SettingsSection"]
  key_links:
    - from: "apps/desktop/src/components/ui/settings-row.tsx"
      to: "@/utils"
      via: "import"
      pattern: "cn.*from.*@/utils"
---

<objective>
Build layout wrapper components: SettingsRow and SettingsSection.

Purpose: Create container components that organize settings with proper spacing and visual hierarchy
Output: Two new UI component files (settings-row.tsx, settings-section.tsx)
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/desktop/src/components/ui/select.tsx
@apps/desktop/src/utils/lib-utils.ts
</context>

<tasks>

<task type="auto">
  <name>UI-03: Create SettingsRow component</name>
  <files>apps/desktop/src/components/ui/settings-row.tsx</files>
  <action>
    Create SettingsRow component for layout of label + description + control.

    Use these imports:
    - Component, ComponentProps from "solid-js"
    - Show from "solid-js"
    - cn from "@/utils"

    Define interface:
    ```
    interface SettingsRowProps extends ComponentProps<"div"> {
      label: string;
      description?: string;
      children: JSX.Element;
    }
    ```

    Implement with Tailwind classes:
    - Container: flex items-center justify-between gap-4 py-3
    - Label side (left): flex-1
    - Label: text-sm font-medium text-foreground
    - Description: text-muted-foreground text-xs (below label)
    - Control side (right): flex-shrink-0

    Note: Do NOT destructure props - use props.label, props.description, props.children directly

  </action>
  <verify>
    File exists at apps/desktop/src/components/ui/settings-row.tsx with SettingsRow export
  </verify>
  <done>
    Settings row displays label on left, description below, and control on right in a single row
  </done>
</task>

<task type="auto">
  <name>UI-04: Create SettingsSection component</name>
  <files>apps/desktop/src/components/ui/settings-section.tsx</files>
  <action>
    Create SettingsSection component for grouping related settings.

    Use these imports:
    - Component, ComponentProps from "solid-js"
    - Show from "solid-js"
    - cn from "@/utils"

    Define interface:
    ```
    interface SettingsSectionProps extends ComponentProps<"div"> {
      title: string;
      description?: string;
      children: JSX.Element;
    }
    ```

    Implement with Tailwind classes:
    - Container: mb-6
    - Header (title area): mb-3, border-b, border-border, pb-2
    - Title: text-sm font-semibold text-foreground
    - Description: text-muted-foreground text-xs (below title)
    - Children container: space-y-1

    Note: Do NOT destructure props - use props.title, props.description, props.children directly

  </action>
  <verify>
    File exists at apps/desktop/src/components/ui/settings-section.tsx with SettingsSection export
  </verify>
  <done>
    Settings section header visually groups related settings with title and optional description
  </done>
</task>

</tasks>

<verification>
- [ ] SettingsRow renders label left, description below, control right
- [ ] SettingsSection renders title with optional description and groups children
</verification>

<success_criteria>
UI-03: Settings row displays label on left, description below, and control on right in a single row
UI-04: Settings section header visually groups related settings with title and optional description
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-components/01-02-SUMMARY.md`
</output>
