---
phase: 01-foundation-ui-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/components/ui/select.tsx
  - apps/desktop/src/components/ui/switch.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Select dropdown renders with dark theme and options are visible on click"
    - "Toggle switch toggles between on/off states with visual feedback"
  artifacts:
    - path: "apps/desktop/src/components/ui/select.tsx"
      provides: "Select dropdown with dark theme matching model-selector aesthetic"
      exports: ["Select", "SelectContent", "SelectItem", "SelectValue"]
    - path: "apps/desktop/src/components/ui/switch.tsx"
      provides: "Toggle switch with dark theme"
      exports: ["Switch"]
  key_links:
    - from: "apps/desktop/src/components/ui/select.tsx"
      to: "@kobalte/core/select"
      via: "import"
      pattern: "import.*@kobalte/core/select"
    - from: "apps/desktop/src/components/ui/switch.tsx"
      to: "@kobalte/core/switch"
      via: "import"
      pattern: "import.*@kobalte/core/switch"
---

<objective>
Build atomic UI components: Select dropdown and Toggle switch with dark theme styling.

Purpose: Create reusable primitives that match the dark-themed aesthetic used in model-selector and provider-settings
Output: Two new UI component files (select.tsx, switch.tsx)
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/desktop/src/components/ui/dialog.tsx
@apps/desktop/src/utils/lib-utils.ts

# Reference patterns from provider-settings.tsx for dark theme styling

</context>

<tasks>

<task type="auto">
  <name>UI-01: Create Select dropdown component</name>
  <files>apps/desktop/src/components/ui/select.tsx</files>
  <action>
    Create Select component using @kobalte/core/select with dark theme styling.

    Use these imports:
    - Select as SelectPrimitive from "@kobalte/core/select"
    - PolymorphicProps from "@kobalte/core/polymorphic"
    - splitProps from "solid-js"
    - For, Show from "solid-js"
    - cn from "@/utils"

    Implement with these components:
    - SelectPrimitive.Root (container)
    - SelectPrimitive.Trigger (button that opens dropdown)
    - SelectPrimitive.Value (displays selected value)
    - SelectPrimitive.Icon (chevron icon)
    - SelectPrimitive.Portal (renders dropdown outside)
    - SelectPrimitive.Content (dropdown panel)
    - SelectPrimitive.Listbox (scrollable options list)
    - SelectPrimitive.Item (individual option)
    - SelectPrimitive.ItemLabel (option text)
    - SelectPrimitive.ItemIndicator (checkmark for selected)

    Use these Tailwind classes (matching model-selector aesthetic):
    - Trigger: bg-background, border-border, rounded-lg, h-10, px-3 py-2, text-sm, hover:border-ring/50, focus:border-ring, focus:ring-4
    - Content: bg-popover, text-popover-foreground, rounded-md, border, shadow-lg, max-h-[300px] overflow-y-auto
    - Item: hover:bg-accent, focus:bg-accent, cursor-pointer, rounded-sm, px-2 py-1.5
    - ItemIndicator: text-primary, mr-2

    Export: Select, SelectContent, SelectItem, SelectValue (Value as simple wrapper around SelectPrimitive.Value)

  </action>
  <verify>
    File exists at apps/desktop/src/components/ui/select.tsx with all exports
  </verify>
  <done>
    Select dropdown renders with dark theme styling and options visible on click
  </done>
</task>

<task type="auto">
  <name>UI-02: Create Switch toggle component</name>
  <files>apps/desktop/src/components/ui/switch.tsx</files>
  <action>
    Create Switch component using @kobalte/core/switch with dark theme styling.

    Use these imports:
    - Switch as SwitchPrimitive from "@kobalte/core/switch"
    - PolymorphicProps from "@kobalte/core/polymorphic"
    - splitProps from "solid-js"
    - cn from "@/utils"

    Implement:
    - SwitchPrimitive.Root (the toggle track)
    - SwitchPrimitive.Thumb (the sliding thumb)

    Use these Tailwind classes:
    - Root: bg-muted, h-6 w-11, rounded-full, border-2 border-transparent, transition-colors, data-[checked]:bg-primary, disabled:cursor-not-allowed disabled:opacity-50
    - Thumb: bg-foreground, h-5 w-5, rounded-full, shadow-lg, transition-transform, data-[checked]:translate-x-5, data-[unchecked]:translate-x-0

    Export: Switch

  </action>
  <verify>
    File exists at apps/desktop/src/components/ui/switch.tsx with Switch export
  </verify>
  <done>
    Toggle switch toggles between on/off states with visual feedback
  </done>
</task>

</tasks>

<verification>
- [ ] Select component renders dark-themed dropdown with options on click
- [ ] Switch component toggles state visually when clicked
- [ ] Both components follow SolidJS patterns (no prop destructuring)
</verification>

<success_criteria>
UI-01: Select dropdown renders with dark theme and options are visible on click
UI-02: Toggle switch toggles between on/off states with visual feedback
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-components/01-01-SUMMARY.md`
</output>
